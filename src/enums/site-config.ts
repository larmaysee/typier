export enum LanguageCode {
  EN = "en",
  MY = "my",
  LI = "li",
}

export enum KeyboardMode {
  DEFAULT = "default",
  PRACTICE = "practice",
}

export enum ThemeMode {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}
